/mode auto
/builtin developer
Execute the following task using shell commands for all Git operations. Create a new branch first with 'git checkout -b goose-task-1762358665'. After making changes and committing, push with 'git push origin HEAD'. Then create the PR with 'gh pr create --title "Goose: Create a hello_world.py file with the following content: print('Hello, World!'). Then, stage the file with git add, commit it with the message 'Add hello world Python program', and create a pull request with title 'Add hello world program' and body 'This PR adds a simple Python hello world script for testing.'" --body "Automated changes by Goose" --base main'. Task: Create a hello_world.py file with the following content: print('Hello, World!'). Then, stage the file with git add, commit it with the message 'Add hello world Python program', and create a pull request with title 'Add hello world program' and body 'This PR adds a simple Python hello world script for testing.'
